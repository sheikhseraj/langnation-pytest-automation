[pytest]
addopts = -ra --strict-markers --tracing retain-on-failure
testpaths = tests_py
markers =
    api: backend API smoke tests
    ui: browser-based UI tests
    smoke: fast build-verification tests for CI
    auth: authentication and protected-flow tests
    pages: page route and render smoke tests
    data: tests that use external test data
